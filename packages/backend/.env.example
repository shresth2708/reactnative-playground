# Example environment variables for the backend
# Copy this file to `.env` in the same folder and fill values before running locally.

# MongoDB connection URI (optional). If omitted, the server will run in in-memory mode.
MONGODB_URI=

# JWT secret used to sign authentication tokens. Replace with a strong secret in production.
JWT_SECRET=change-me-to-a-strong-secret

# Google Gemini API key for AI features (optional). Leave empty to disable Gemini calls.
GEMINI_API_KEY=

# Port the backend server listens on (default: 4000)
PORT=4000
# Server Configuration
PORT=4000
NODE_ENV=production

# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# AI API Key (Gemini)
GEMINI_API_KEY=your-gemini-api-key-here

# Frontend URL (for CORS)
FRONTEND_URL=https://nsthack-frontend-azey.vercel.app
